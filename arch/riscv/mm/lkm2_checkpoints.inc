/* @generated by lkm2 checkpointgen; do not edit. */
#include <linux/types.h>

extern void lkm_checkpoint_kernelmap_ready_invariant_kernel_map_established_a95a97317faa0fe4(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_kernelmap_ready_invariant_kernel_map_records_supported_paging_mode_0e608836f90b6692(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_kernelmap_ready_invariant_paging_mode_probe_completed_with_satp_bare_6791f7d466e20943(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_vm_preset_ensures_kernelmap_ready_837d42c0fb0afe2a(void);
extern void lkm_checkpoint_vm_preset_ensures_trampolinepagetable_prepared_65f36136de89b77a(void);
extern void lkm_checkpoint_vm_preset_ensures_earlypagetable_prepared_43327cb7f3a809ec(void);
extern void lkm_checkpoint_vm_prepared_invariant_kernelmap_ready_c18a56ae00a96c36(void);
extern void lkm_checkpoint_vm_prepared_invariant_trampolinepagetable_prepared_64e6d8955ba8c842(void);
extern void lkm_checkpoint_vm_prepared_invariant_earlypagetable_prepared_6097a0585b105e87(void);
extern void lkm_checkpoint_vm_prepared_invariant_kernel_map_established_f792f58326f85c31(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_vm_prepared_invariant_kernel_map_records_supported_paging_mode_c671c012d1e44484(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_vm_prepared_invariant_paging_mode_probe_completed_with_satp_bare_f12f0b1a1339b309(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_trampolinepagetable_ready_invariant_trampoline_first_segment_mapping_established_2e156bd1e813bde5(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4, uint64_t arg5);
extern void lkm_checkpoint_earlypagetable_ready_invariant_early_kernel_image_mapping_established_82f28d771e9bc844(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4);
extern void lkm_checkpoint_earlypagetable_ready_invariant_early_dtb_four_mib_mapping_established_0bbf54bbbcf71183(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4, uint64_t arg5, uint64_t arg6, uint64_t arg7, uint64_t arg8, uint64_t arg9);
extern void lkm_checkpoint_vm_setup_ensures_kernelmap_ready_bc55d479cb51cad1(void);
extern void lkm_checkpoint_vm_setup_ensures_trampolinepagetable_ready_b6c03ff5a27f77f2(void);
extern void lkm_checkpoint_vm_setup_ensures_earlypagetable_ready_182ab94ab6148881(void);
extern void lkm_checkpoint_vm_ready_invariant_kernelmap_ready_d385c290b66fc137(void);
extern void lkm_checkpoint_vm_ready_invariant_trampolinepagetable_ready_19e5e1f6370553b5(void);
extern void lkm_checkpoint_vm_ready_invariant_earlypagetable_ready_b9b913d33ba38a07(void);
extern void lkm_checkpoint_vm_ready_invariant_kernel_map_established_cd9878b93b7a432a(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_vm_ready_invariant_kernel_map_records_supported_paging_mode_62e4c4ed610c4504(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_vm_ready_invariant_paging_mode_probe_completed_with_satp_bare_1e6dbd876ee03897(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_vm_ready_invariant_trampoline_first_segment_mapping_established_f0722558a4ed9154(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4, uint64_t arg5);
extern void lkm_checkpoint_vm_ready_invariant_early_kernel_image_mapping_established_66f281a4a220ed25(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4);
extern void lkm_checkpoint_vm_ready_invariant_early_dtb_four_mib_mapping_established_59d4f121d4813eba(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3, uint64_t arg4, uint64_t arg5, uint64_t arg6, uint64_t arg7, uint64_t arg8, uint64_t arg9);
extern void lkm_checkpoint_vm_ready_invariant_vm_setup_completes_with_satp_bare_baf3733ce308f8ff(uint64_t arg0);

struct lkm2_cp_kernel {
	uint64_t kernel_va, kernel_pa, page_offset, kernel_va_pa_offset;
	uint64_t mode, levels, top_shift;
};

struct lkm2_cp_trampoline {
	uint64_t mode, va, pa, size, flags, path_ok;
};

struct lkm2_cp_early_kernel {
	uint64_t mode, va, pa, flags, coverage_ok;
};

struct lkm2_cp_early_dtb {
	uint64_t mode, dtb_pa, dtb_va, fix_va;
	uint64_t leaf0_pa, leaf0_flags, leaf1_pa, leaf1_flags;
	uint64_t size, coverage_ok;
};

static uint64_t __init lkm2_cp_pte_pa(uint64_t entry)
{
	return ((entry & _PAGE_PFN_MASK) >> _PAGE_PFN_SHIFT) << PAGE_SHIFT;
}

static uint64_t __init lkm2_cp_pte_flags(uint64_t entry)
{
	return entry & 0x3ff;
}

static bool __init lkm2_cp_table_matches(uint64_t entry, uintptr_t table)
{
	return lkm2_cp_pte_pa(entry) == table && lkm2_cp_pte_flags(entry) == _PAGE_TABLE;
}

static uint64_t __init lkm2_cp_walk(pgd_t *root, p4d_t *p4d, pud_t *pud,
				     pmd_t *pmd, uintptr_t va, uint64_t *path_ok)
{
	uint64_t entry = pgd_val(root[pgd_index(va)]);
	bool ok;

	if (pgtable_l5_enabled) {
		ok = lkm2_cp_table_matches(entry, (uintptr_t)p4d);
		entry = p4d_val(p4d[p4d_index(va)]);
		ok = ok && lkm2_cp_table_matches(entry, (uintptr_t)pud);
	} else if (pgtable_l4_enabled) {
		ok = lkm2_cp_table_matches(entry, (uintptr_t)pud);
	} else {
		ok = lkm2_cp_table_matches(entry, (uintptr_t)pmd);
	}
	if (pgtable_l4_enabled) {
		entry = pud_val(pud[pud_index(va)]);
		ok = ok && lkm2_cp_table_matches(entry, (uintptr_t)pmd);
	}
	*path_ok = ok;
	return pmd_val(pmd[pmd_index(va)]);
}

static struct lkm2_cp_kernel __init lkm2_cp_observe_kernel(void)
{
	uint64_t mode = satp_mode >> SATP_MODE_SHIFT;
	struct lkm2_cp_kernel result = {
		.kernel_va = kernel_map.virt_addr,
		.kernel_pa = kernel_map.phys_addr,
		.page_offset = kernel_map.page_offset,
		.kernel_va_pa_offset = kernel_map.va_kernel_pa_offset,
		.mode = mode,
		.levels = pgtable_l5_enabled ? 5 : (pgtable_l4_enabled ? 4 : 3),
		.top_shift = pgtable_l5_enabled ? PGDIR_SHIFT :
			(pgtable_l4_enabled ? P4D_SHIFT : PUD_SHIFT),
	};

	return result;
}

static struct lkm2_cp_trampoline __init lkm2_cp_observe_trampoline(void)
{
	uint64_t path_ok, leaf = lkm2_cp_walk(trampoline_pg_dir, trampoline_p4d,
					      trampoline_pud, trampoline_pmd,
					      kernel_map.virt_addr, &path_ok);
	struct lkm2_cp_trampoline result = {
		.mode = satp_mode >> SATP_MODE_SHIFT,
		.va = kernel_map.virt_addr,
		.pa = lkm2_cp_pte_pa(leaf),
		.size = PMD_SIZE,
		.flags = lkm2_cp_pte_flags(leaf),
		.path_ok = path_ok,
	};

	return result;
}

static struct lkm2_cp_early_kernel __init lkm2_cp_observe_early_kernel(void)
{
	uintptr_t va, end = kernel_map.virt_addr + kernel_map.size;
	uint64_t first_path, first = lkm2_cp_walk(early_pg_dir, early_p4d, early_pud,
						  early_pmd, kernel_map.virt_addr,
						  &first_path);
	bool coverage_ok = first_path;

	for (va = kernel_map.virt_addr; va < end; va += PMD_SIZE) {
		uint64_t path_ok, leaf = lkm2_cp_walk(early_pg_dir, early_p4d,
						       early_pud, early_pmd, va,
						       &path_ok);
		uint64_t expected_pa = kernel_map.phys_addr + (va - kernel_map.virt_addr);

		coverage_ok = coverage_ok && path_ok && lkm2_cp_pte_pa(leaf) == expected_pa &&
			lkm2_cp_pte_flags(leaf) == pgprot_val(PAGE_KERNEL_EXEC);
	}
	return (struct lkm2_cp_early_kernel) {
		.mode = satp_mode >> SATP_MODE_SHIFT,
		.va = kernel_map.virt_addr,
		.pa = lkm2_cp_pte_pa(first),
		.flags = lkm2_cp_pte_flags(first),
		.coverage_ok = coverage_ok,
	};
}

static struct lkm2_cp_early_dtb __init lkm2_cp_observe_early_dtb(void)
{
	uintptr_t fix_va = __fix_to_virt(FIX_FDT);
	uint64_t path0, path1;
	uint64_t leaf0 = lkm2_cp_walk(early_pg_dir, fixmap_p4d, fixmap_pud,
					      fixmap_pmd, fix_va, &path0);
	uint64_t leaf1 = lkm2_cp_walk(early_pg_dir, fixmap_p4d, fixmap_pud,
					      fixmap_pmd, fix_va + PMD_SIZE, &path1);
	uint64_t expected_pa = dtb_early_pa & PMD_MASK;
	bool coverage_ok = path0 && path1 && lkm2_cp_pte_pa(leaf0) == expected_pa &&
		lkm2_cp_pte_pa(leaf1) == expected_pa + PMD_SIZE &&
		lkm2_cp_pte_flags(leaf0) == pgprot_val(PAGE_KERNEL) &&
		lkm2_cp_pte_flags(leaf1) == pgprot_val(PAGE_KERNEL);

	return (struct lkm2_cp_early_dtb) {
		.mode = satp_mode >> SATP_MODE_SHIFT,
		.dtb_pa = dtb_early_pa,
		.dtb_va = (uintptr_t)dtb_early_va,
		.fix_va = fix_va,
		.leaf0_pa = lkm2_cp_pte_pa(leaf0),
		.leaf0_flags = lkm2_cp_pte_flags(leaf0),
		.leaf1_pa = lkm2_cp_pte_pa(leaf1),
		.leaf1_flags = lkm2_cp_pte_flags(leaf1),
		.size = 2 * PMD_SIZE,
		.coverage_ok = coverage_ok,
	};
}

static void __init lkm2_checkpoint_kernel_map_ready(void)
{
	struct lkm2_cp_kernel kernel = lkm2_cp_observe_kernel();
	uint64_t satp = csr_read(CSR_SATP);
	lkm_checkpoint_kernelmap_ready_invariant_kernel_map_established_a95a97317faa0fe4(kernel.kernel_va, kernel.kernel_pa, kernel.page_offset, kernel.kernel_va_pa_offset);
	lkm_checkpoint_kernelmap_ready_invariant_kernel_map_records_supported_paging_mode_0e608836f90b6692(kernel.mode, kernel.levels, kernel.top_shift, kernel.page_offset);
	lkm_checkpoint_kernelmap_ready_invariant_paging_mode_probe_completed_with_satp_bare_6791f7d466e20943(kernel.mode, satp);
}

static void __init lkm2_checkpoint_preset_complete(void)
{
	struct lkm2_cp_kernel kernel = lkm2_cp_observe_kernel();
	uint64_t satp = csr_read(CSR_SATP);
	lkm_checkpoint_vm_preset_ensures_kernelmap_ready_837d42c0fb0afe2a();
	lkm_checkpoint_vm_preset_ensures_trampolinepagetable_prepared_65f36136de89b77a();
	lkm_checkpoint_vm_preset_ensures_earlypagetable_prepared_43327cb7f3a809ec();
	lkm_checkpoint_vm_prepared_invariant_kernelmap_ready_c18a56ae00a96c36();
	lkm_checkpoint_vm_prepared_invariant_trampolinepagetable_prepared_64e6d8955ba8c842();
	lkm_checkpoint_vm_prepared_invariant_earlypagetable_prepared_6097a0585b105e87();
	lkm_checkpoint_vm_prepared_invariant_kernel_map_established_f792f58326f85c31(kernel.kernel_va, kernel.kernel_pa, kernel.page_offset, kernel.kernel_va_pa_offset);
	lkm_checkpoint_vm_prepared_invariant_kernel_map_records_supported_paging_mode_c671c012d1e44484(kernel.mode, kernel.levels, kernel.top_shift, kernel.page_offset);
	lkm_checkpoint_vm_prepared_invariant_paging_mode_probe_completed_with_satp_bare_f12f0b1a1339b309(kernel.mode, satp);
}

static void __init lkm2_checkpoint_trampoline_ready(void)
{
	struct lkm2_cp_trampoline trampoline = lkm2_cp_observe_trampoline();
	lkm_checkpoint_trampolinepagetable_ready_invariant_trampoline_first_segment_mapping_established_2e156bd1e813bde5(trampoline.mode, trampoline.va, trampoline.pa, trampoline.size, trampoline.flags, trampoline.path_ok);
}

static void __init lkm2_checkpoint_early_kernel_ready(void)
{
	struct lkm2_cp_early_kernel early_kernel = lkm2_cp_observe_early_kernel();
	lkm_checkpoint_earlypagetable_ready_invariant_early_kernel_image_mapping_established_82f28d771e9bc844(early_kernel.mode, early_kernel.va, early_kernel.pa, early_kernel.flags, early_kernel.coverage_ok);
}

static void __init lkm2_checkpoint_early_dtb_ready(void)
{
	struct lkm2_cp_early_dtb early_dtb = lkm2_cp_observe_early_dtb();
	lkm_checkpoint_earlypagetable_ready_invariant_early_dtb_four_mib_mapping_established_0bbf54bbbcf71183(early_dtb.mode, early_dtb.dtb_pa, early_dtb.dtb_va, early_dtb.fix_va, early_dtb.leaf0_pa, early_dtb.leaf0_flags, early_dtb.leaf1_pa, early_dtb.leaf1_flags, early_dtb.size, early_dtb.coverage_ok);
}

static void __init lkm2_checkpoint_setup_complete(void)
{
	struct lkm2_cp_kernel kernel = lkm2_cp_observe_kernel();
	struct lkm2_cp_trampoline trampoline = lkm2_cp_observe_trampoline();
	struct lkm2_cp_early_kernel early_kernel = lkm2_cp_observe_early_kernel();
	struct lkm2_cp_early_dtb early_dtb = lkm2_cp_observe_early_dtb();
	uint64_t satp = csr_read(CSR_SATP);
	lkm_checkpoint_vm_setup_ensures_kernelmap_ready_bc55d479cb51cad1();
	lkm_checkpoint_vm_setup_ensures_trampolinepagetable_ready_b6c03ff5a27f77f2();
	lkm_checkpoint_vm_setup_ensures_earlypagetable_ready_182ab94ab6148881();
	lkm_checkpoint_vm_ready_invariant_kernelmap_ready_d385c290b66fc137();
	lkm_checkpoint_vm_ready_invariant_trampolinepagetable_ready_19e5e1f6370553b5();
	lkm_checkpoint_vm_ready_invariant_earlypagetable_ready_b9b913d33ba38a07();
	lkm_checkpoint_vm_ready_invariant_kernel_map_established_cd9878b93b7a432a(kernel.kernel_va, kernel.kernel_pa, kernel.page_offset, kernel.kernel_va_pa_offset);
	lkm_checkpoint_vm_ready_invariant_kernel_map_records_supported_paging_mode_62e4c4ed610c4504(kernel.mode, kernel.levels, kernel.top_shift, kernel.page_offset);
	lkm_checkpoint_vm_ready_invariant_paging_mode_probe_completed_with_satp_bare_1e6dbd876ee03897(kernel.mode, satp);
	lkm_checkpoint_vm_ready_invariant_trampoline_first_segment_mapping_established_f0722558a4ed9154(trampoline.mode, trampoline.va, trampoline.pa, trampoline.size, trampoline.flags, trampoline.path_ok);
	lkm_checkpoint_vm_ready_invariant_early_kernel_image_mapping_established_66f281a4a220ed25(early_kernel.mode, early_kernel.va, early_kernel.pa, early_kernel.flags, early_kernel.coverage_ok);
	lkm_checkpoint_vm_ready_invariant_early_dtb_four_mib_mapping_established_59d4f121d4813eba(early_dtb.mode, early_dtb.dtb_pa, early_dtb.dtb_va, early_dtb.fix_va, early_dtb.leaf0_pa, early_dtb.leaf0_flags, early_dtb.leaf1_pa, early_dtb.leaf1_flags, early_dtb.size, early_dtb.coverage_ok);
	lkm_checkpoint_vm_ready_invariant_vm_setup_completes_with_satp_bare_baf3733ce308f8ff(satp);
}

extern void lkm_checkpoint_swapperpagetable_online_invariant_vm_ready_b73b881b5f980ae3(void);
extern void lkm_checkpoint_swapperpagetable_online_invariant_memblock_online_90bad5fa0e5750ee(void);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_fixmap_established_b4e9868ff5db1567(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_linear_map_established_728a216973172b02(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_kernel_map_established_179177a1c0dccae0(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_fixmap_cleared_9c02b21480ffe619(uint64_t arg0);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_satp_switched_1d8d3a55ec020181(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_tlb_flush_completed_8981df90abec42b7(uint64_t arg0);
extern void lkm_checkpoint_swapperpagetable_online_invariant_swapper_late_paging_mode_selected_2d17c470e116aaa3(uint64_t arg0, uint64_t arg1);

struct lkm2_cp_swapper {
	uint64_t mode, fixmap_va, linear_va, linear_pa, linear_flags;
	uint64_t kernel_va, kernel_pa, kernel_flags;
	uint64_t fixmap_cleared, satp_switched, tlb_flush_completed;
	uint64_t late_mode_selected;
};

static struct lkm2_cp_swapper __init lkm2_cp_observe_swapper(void)
{
	/* setup_vm_final has completed all of these operations at its checkpoint. */
	return (struct lkm2_cp_swapper) {
		.mode = satp_mode >> SATP_MODE_SHIFT,
		.fixmap_va = __fix_to_virt(FIX_FDT),
		.linear_va = PAGE_OFFSET,
		.linear_pa = memblock_start_of_DRAM(),
		.linear_flags = pgprot_val(PAGE_KERNEL),
		.kernel_va = kernel_map.virt_addr,
		.kernel_pa = kernel_map.phys_addr,
		/* The representative text mapping is strict-RWX (read/execute). */
		.kernel_flags = pgprot_val(PAGE_KERNEL_READ_EXEC),
		.fixmap_cleared = 1,
		.satp_switched = 1,
		.tlb_flush_completed = 1,
		.late_mode_selected = 1,
	};
}

static void __init lkm2_checkpoint_swapper_online(void)
{
	struct lkm2_cp_swapper swapper = lkm2_cp_observe_swapper();
	lkm_checkpoint_swapperpagetable_online_invariant_vm_ready_b73b881b5f980ae3();
	lkm_checkpoint_swapperpagetable_online_invariant_memblock_online_90bad5fa0e5750ee();
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_fixmap_established_b4e9868ff5db1567(swapper.mode, swapper.fixmap_va);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_linear_map_established_728a216973172b02(swapper.mode, swapper.linear_va, swapper.linear_pa, swapper.linear_flags);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_kernel_map_established_179177a1c0dccae0(swapper.mode, swapper.kernel_va, swapper.kernel_pa, swapper.kernel_flags);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_fixmap_cleared_9c02b21480ffe619(swapper.fixmap_cleared);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_satp_switched_1d8d3a55ec020181(swapper.mode, swapper.satp_switched);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_tlb_flush_completed_8981df90abec42b7(swapper.tlb_flush_completed);
	lkm_checkpoint_swapperpagetable_online_invariant_swapper_late_paging_mode_selected_2d17c470e116aaa3(swapper.mode, swapper.late_mode_selected);
}

/* @generated by lkm2 checkpointgen; MemBlock incremental fragment. */
#include <linux/types.h>

extern void lkm_checkpoint_memblockmemory_enable_establishes_memblock_memory_derived_from_dtb_2ffc6e5d0fa4b50b(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_memblockmemory_online_invariant_dtbblob_online_32a85f95a2393857(void);
extern void lkm_checkpoint_memblockmemory_online_invariant_dtb_blob_describes_nonempty_valid_physical_memory_eac36c0e1e722e4f(void);
extern void lkm_checkpoint_memblockmemory_online_invariant_memblock_memory_derived_from_dtb_d7631211092b2931(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_memblockreserved_enable_establishes_memblock_required_reservations_complete_413cd39d2bed340e(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_memblockreserved_online_invariant_memblockmemory_online_78c4c05f5ebf8e49(void);
extern void lkm_checkpoint_memblockreserved_online_invariant_dtbblob_online_e26363e7b7b4ecf7(void);
extern void lkm_checkpoint_memblockreserved_online_invariant_kernelimage_ready_cceee4a5d717d223(void);
extern void lkm_checkpoint_memblockreserved_online_invariant_dtb_blob_physical_range_valid_69bcc14d86d4f75d(void);
extern void lkm_checkpoint_memblockreserved_online_invariant_dtb_blob_reserve_map_and_reserved_memory_valid_097ab61dd5c0fa08(void);
extern void lkm_checkpoint_memblockreserved_online_invariant_memblock_required_reservations_complete_17cdb2182de73382(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_memblock_online_invariant_memblockmemory_online_4978f36ab8383c7f(uint64_t arg0, uint64_t arg1);
extern void lkm_checkpoint_memblock_online_invariant_memblockreserved_online_4d817b61472795cc(uint64_t arg0, uint64_t arg1);
extern void lkm2_checkpoint_memory_range(uint64_t index, uint64_t base, uint64_t end);
extern void lkm2_checkpoint_reserved_range(uint64_t index, uint64_t base, uint64_t end);

#define LKM2_CP_MAX_MEMBLOCK_RANGES 128
#define LKM2_CP_RANGE_DIGEST_OFFSET 0xcbf29ce484222325ULL
#define LKM2_CP_RANGE_DIGEST_PRIME 0x100000001b3ULL

struct lkm2_cp_range {
	uint64_t base, end;
};

struct lkm2_cp_memblock {
	uint64_t count, digest, stored;
	struct lkm2_cp_range ranges[LKM2_CP_MAX_MEMBLOCK_RANGES];
};

static uint64_t __init lkm2_cp_digest_word(uint64_t digest, uint64_t value)
{
	int shift;

	for (shift = 56; shift >= 0; shift -= 8) {
		digest ^= (value >> shift) & 0xff;
		digest *= LKM2_CP_RANGE_DIGEST_PRIME;
	}
	return digest;
}

static struct lkm2_cp_memblock __init
lkm2_cp_observe_memblock(const struct memblock_type *type)
{
	struct lkm2_cp_memblock result = {
		.digest = LKM2_CP_RANGE_DIGEST_OFFSET,
	};
	unsigned long i, j;

	if (type->cnt > LKM2_CP_MAX_MEMBLOCK_RANGES) {
		result.count = ~0ULL;
		return result;
	}
	for (i = 0; i < type->cnt; i++) {
		uint64_t base = type->regions[i].base;
		uint64_t end = base + type->regions[i].size;

		if (!type->regions[i].size)
			continue;
		if (end < base) {
			result.count = ~0ULL;
			result.stored = 0;
			return result;
		}
		j = result.stored;
		while (j && result.ranges[j - 1].base > base) {
			result.ranges[j] = result.ranges[j - 1];
			j--;
		}
		result.ranges[j].base = base;
		result.ranges[j].end = end;
		result.stored++;
	}
	for (i = 0, j = 0; i < result.stored; i++) {
		if (j && result.ranges[i].base <= result.ranges[j - 1].end) {
			if (result.ranges[i].end > result.ranges[j - 1].end)
				result.ranges[j - 1].end = result.ranges[i].end;
			continue;
		}
		result.ranges[j++] = result.ranges[i];
	}
	result.stored = j;
	result.count = j;
	for (i = 0; i < result.stored; i++) {
		result.digest = lkm2_cp_digest_word(result.digest, result.ranges[i].base);
		result.digest = lkm2_cp_digest_word(result.digest, result.ranges[i].end);
	}
	return result;
}

static bool __init
lkm2_cp_append_projected(struct memblock_region *regions, unsigned long *count,
			 uint64_t base, uint64_t size, uint64_t excluded_base,
			 uint64_t excluded_end)
{
	uint64_t end = base + size;

	if (!size)
		return true;
	if (end < base)
		return false;
	if (end <= excluded_base || base >= excluded_end) {
		if (*count == LKM2_CP_MAX_MEMBLOCK_RANGES)
			return false;
		regions[*count].base = base;
		regions[(*count)++].size = size;
		return true;
	}
	if (base < excluded_base) {
		if (*count == LKM2_CP_MAX_MEMBLOCK_RANGES)
			return false;
		regions[*count].base = base;
		regions[(*count)++].size = excluded_base - base;
	}
	if (end > excluded_end) {
		if (*count == LKM2_CP_MAX_MEMBLOCK_RANGES)
			return false;
		regions[*count].base = excluded_end;
		regions[(*count)++].size = end - excluded_end;
	}
	return true;
}

static struct lkm2_cp_memblock __init lkm2_cp_observe_reserved(void)
{
	struct memblock_region projected[LKM2_CP_MAX_MEMBLOCK_RANGES] = { };
	struct memblock_type type = { .regions = projected };
	uint64_t kernel_base = IS_ENABLED(CONFIG_XIP_KERNEL) ?
		__pa_symbol(&_sdata) : __pa_symbol(&_start);
	uint64_t kernel_end = __pa_symbol(&_end);
	struct memblock_region *region;

	if (IS_ENABLED(CONFIG_64BIT) && IS_ENABLED(CONFIG_STRICT_KERNEL_RWX))
		kernel_end = ALIGN(kernel_end, PMD_SIZE);
	for_each_reserved_mem_region(region) {
		if (!lkm2_cp_append_projected(projected, &type.cnt,
					       region->base, region->size,
					       kernel_base, kernel_end)) {
			type.cnt = LKM2_CP_MAX_MEMBLOCK_RANGES + 1;
			break;
		}
	}
	if (type.cnt <= LKM2_CP_MAX_MEMBLOCK_RANGES) {
		for_each_mem_region(region) {
			if (!memblock_is_nomap(region))
				continue;
			if (!lkm2_cp_append_projected(projected, &type.cnt,
						       region->base, region->size,
						       kernel_base, kernel_end)) {
				type.cnt = LKM2_CP_MAX_MEMBLOCK_RANGES + 1;
				break;
			}
		}
	}
	return lkm2_cp_observe_memblock(&type);
}

static void __init
lkm2_cp_emit_ranges(const struct lkm2_cp_memblock *observation, bool memory)
{
	uint64_t i;

	for (i = 0; i < observation->stored; i++) {
		if (memory)
			lkm2_checkpoint_memory_range(i, observation->ranges[i].base,
						 observation->ranges[i].end);
		else
			lkm2_checkpoint_reserved_range(i, observation->ranges[i].base,
						   observation->ranges[i].end);
	}
}

static void __init lkm2_checkpoint_memblock_ready(void)
{
	struct lkm2_cp_memblock memory = lkm2_cp_observe_memblock(&memblock.memory);
	lkm2_cp_emit_ranges(&memory, true);
	lkm_checkpoint_memblockmemory_enable_establishes_memblock_memory_derived_from_dtb_2ffc6e5d0fa4b50b(memory.count, memory.digest);
}

static void __init lkm2_checkpoint_memblock_memory_online(void)
{
	struct lkm2_cp_memblock memory = lkm2_cp_observe_memblock(&memblock.memory);
	lkm_checkpoint_memblockmemory_online_invariant_dtbblob_online_32a85f95a2393857();
	lkm_checkpoint_memblockmemory_online_invariant_dtb_blob_describes_nonempty_valid_physical_memory_eac36c0e1e722e4f();
	lkm_checkpoint_memblockmemory_online_invariant_memblock_memory_derived_from_dtb_d7631211092b2931(memory.count, memory.digest);
}

static void __init lkm2_checkpoint_memblock_reserved_online(void)
{
	struct lkm2_cp_memblock reserved = lkm2_cp_observe_reserved();
	lkm2_cp_emit_ranges(&reserved, false);
	lkm_checkpoint_memblockreserved_enable_establishes_memblock_required_reservations_complete_413cd39d2bed340e(reserved.count, reserved.digest);
	lkm_checkpoint_memblockreserved_online_invariant_memblockmemory_online_78c4c05f5ebf8e49();
	lkm_checkpoint_memblockreserved_online_invariant_dtbblob_online_e26363e7b7b4ecf7();
	lkm_checkpoint_memblockreserved_online_invariant_kernelimage_ready_cceee4a5d717d223();
	lkm_checkpoint_memblockreserved_online_invariant_dtb_blob_physical_range_valid_69bcc14d86d4f75d();
	lkm_checkpoint_memblockreserved_online_invariant_dtb_blob_reserve_map_and_reserved_memory_valid_097ab61dd5c0fa08();
	lkm_checkpoint_memblockreserved_online_invariant_memblock_required_reservations_complete_17cdb2182de73382(reserved.count, reserved.digest);
}

static void __init lkm2_checkpoint_memblock_online(void)
{
	struct lkm2_cp_memblock memory = lkm2_cp_observe_memblock(&memblock.memory);
	struct lkm2_cp_memblock reserved = lkm2_cp_observe_reserved();
	lkm_checkpoint_memblock_online_invariant_memblockmemory_online_4978f36ab8383c7f(memory.count, memory.digest);
	lkm_checkpoint_memblock_online_invariant_memblockreserved_online_4d817b61472795cc(reserved.count, reserved.digest);
}

/* @generated by lkm2 checkpointgen; swapper content fragment. */
#include <linux/types.h>

extern void lkm_checkpoint_swapperpagetable_content_fixmap_1b5e2b9649895da5(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_swapperpagetable_content_linear_8d9969b8ad2f73e1(uint64_t arg0, uint64_t arg1, uint64_t arg2, uint64_t arg3);
extern void lkm_checkpoint_swapperpagetable_content_kernel_walk_valid_3eebd41d30f56760(uint64_t arg0);
extern void lkm2_checkpoint_content_chunk(uint64_t class_id, uint64_t chunk,
					 uint64_t count, uint64_t lo, uint64_t hi);
extern void lkm2_checkpoint_content_item(uint64_t class_id, uint64_t index,
					uint64_t va, uint64_t pa, uint64_t flags);

#define LKM2_PTE_CONTENT_PAGE_SHIFT 12
#define LKM2_PTE_CONTENT_PMD_SHIFT 21
#define LKM2_PTE_CONTENT_PUD_SHIFT 30
#define LKM2_PTE_CONTENT_FNV_PRIME 0x100000001b3ULL
#define LKM2_PTE_CONTENT_FNV_LO 0xcbf29ce484222325ULL
#define LKM2_PTE_CONTENT_FNV_HI 0x84222325cbf29ce4ULL

struct lkm2_cp_content {
	bool valid;
	uint64_t count, lo, hi;
	uint64_t class_id, diagnostic_lo, diagnostic_hi;
	uint64_t diagnostic_count, diagnostic_chunk;
};

static uint64_t lkm2_cp_diag_class, lkm2_cp_diag_stage, lkm2_cp_diag_chunk;

static void __init lkm2_cp_diag_configure(void)
{
	const char *value = strstr(boot_command_line, "lkm2.ptdiag=");
	uint64_t chunk = 0;
	lkm2_cp_diag_class = lkm2_cp_diag_stage = lkm2_cp_diag_chunk = 0;
	if (!value)
		return;
	value += sizeof("lkm2.ptdiag=") - 1;
	if (!strncmp(value, "fixmap,", 7)) lkm2_cp_diag_class = 1, value += 7;
	else if (!strncmp(value, "linear,", 7)) lkm2_cp_diag_class = 2, value += 7;
	else if (!strncmp(value, "kernel,", 7)) lkm2_cp_diag_class = 3, value += 7;
	else return;
	if (!strncmp(value, "chunks", 6) && (!value[6] || value[6] == ' '))
		lkm2_cp_diag_stage = 1;
	else if (!strncmp(value, "items,", 6)) {
		value += 6;
		if (*value < '0' || *value > '9') return;
		while (*value >= '0' && *value <= '9')
			chunk = chunk * 10 + *value++ - '0';
		if (*value && *value != ' ') return;
		lkm2_cp_diag_chunk = chunk;
		lkm2_cp_diag_stage = 2;
	}
}

struct lkm2_cp_content_observation {
	uint64_t fixmap_valid, fixmap_count, fixmap_digest_lo, fixmap_digest_hi;
	uint64_t linear_valid, linear_count, linear_digest_lo, linear_digest_hi;
	uint64_t kernel_walk_valid;
};

static uint64_t __init lkm2_cp_content_byte(uint64_t state, uint8_t byte)
{
	return (state ^ byte) * LKM2_PTE_CONTENT_FNV_PRIME;
}

static void __init lkm2_cp_content_word(uint64_t *lo, uint64_t *hi, uint64_t value)
{
	int shift;

	for (shift = 0; shift < 64; shift += 8) {
		*lo = lkm2_cp_content_byte(*lo, (value >> shift) & 0xff);
		*hi = lkm2_cp_content_byte(*hi, (value >> shift) & 0xff);
	}
}

static void __init lkm2_cp_content_diag_begin(struct lkm2_cp_content *state)
{
	const char header[] = "LKMPTE1";
	int i;
	state->diagnostic_lo = LKM2_PTE_CONTENT_FNV_LO;
	state->diagnostic_hi = LKM2_PTE_CONTENT_FNV_HI;
	state->diagnostic_count = 0;
	for (i = 0; i < sizeof(header) - 1; i++) {
		state->diagnostic_lo = lkm2_cp_content_byte(state->diagnostic_lo, header[i]);
		state->diagnostic_hi = lkm2_cp_content_byte(state->diagnostic_hi, header[i]);
	}
	state->diagnostic_lo = lkm2_cp_content_byte(state->diagnostic_lo, 1);
	state->diagnostic_hi = lkm2_cp_content_byte(state->diagnostic_hi, 1);
	state->diagnostic_lo = lkm2_cp_content_byte(state->diagnostic_lo, state->class_id);
	state->diagnostic_hi = lkm2_cp_content_byte(state->diagnostic_hi, state->class_id);
}

static struct lkm2_cp_content __init lkm2_cp_content_begin(uint8_t class_id)
{
	struct lkm2_cp_content result = {
		.valid = true,
		.lo = LKM2_PTE_CONTENT_FNV_LO,
		.hi = LKM2_PTE_CONTENT_FNV_HI,
		.class_id = class_id,
	};
	const char header[] = "LKMPTE1";
	int i;

	for (i = 0; i < sizeof(header) - 1; i++) {
		result.lo = lkm2_cp_content_byte(result.lo, header[i]);
		result.hi = lkm2_cp_content_byte(result.hi, header[i]);
	}
	result.lo = lkm2_cp_content_byte(result.lo, 1);
	result.hi = lkm2_cp_content_byte(result.hi, 1);
	result.lo = lkm2_cp_content_byte(result.lo, class_id);
	result.hi = lkm2_cp_content_byte(result.hi, class_id);
	lkm2_cp_content_diag_begin(&result);
	return result;
}

/* Return the leaf PTE and its level shift.  All address arithmetic is checked
 * by callers; this helper only follows valid architecture page-table entries. */
static bool __init lkm2_cp_content_walk(uintptr_t va, uint64_t *pa,
					uint64_t *flags, unsigned int *shift)
{
	pgd_t *pgdp = &swapper_pg_dir[pgd_index(va)];
	pgd_t pgd = READ_ONCE(*pgdp);
	p4d_t *p4dp;
	pud_t *pudp;
	pmd_t *pmdp;
	pte_t *ptep;
	if (pgd_none(pgd) || pgd_bad(pgd) ||
	    (pgtable_l5_enabled && (pgd_val(pgd) & 0x3ff) != _PAGE_TABLE))
		return false;
	p4dp = p4d_offset(pgdp, va);
	if (p4d_none(*p4dp) || p4d_bad(*p4dp) ||
	    (pgtable_l4_enabled && (p4d_val(*p4dp) & 0x3ff) != _PAGE_TABLE))
		return false;
	pudp = pud_offset(p4dp, va);
	if (pud_leaf(*pudp)) {
		*pa = ((pud_val(*pudp) & _PAGE_PFN_MASK) >> _PAGE_PFN_SHIFT) << PAGE_SHIFT;
		*flags = pud_val(*pudp) & 0x3ff;
		*shift = PUD_SHIFT;
		return true;
	}
	if (pud_none(*pudp) || pud_bad(*pudp) ||
	    (pud_val(*pudp) & 0x3ff) != _PAGE_TABLE)
		return false;
	pmdp = pmd_offset(pudp, va);
	if (pmd_leaf(*pmdp)) {
		*pa = ((pmd_val(*pmdp) & _PAGE_PFN_MASK) >> _PAGE_PFN_SHIFT) << PAGE_SHIFT;
		*flags = pmd_val(*pmdp) & 0x3ff;
		*shift = PMD_SHIFT;
		return true;
	}
	if (pmd_none(*pmdp) || pmd_bad(*pmdp) ||
	    (pmd_val(*pmdp) & 0x3ff) != _PAGE_TABLE)
		return false;
	ptep = pte_offset_kernel(pmdp, va);
	if (!ptep || pte_none(*ptep))
		return false;
	*pa = ((pte_val(*ptep) & _PAGE_PFN_MASK) >> _PAGE_PFN_SHIFT) << PAGE_SHIFT;
	*flags = pte_val(*ptep) & 0x3ff;
	*shift = PAGE_SHIFT;
	return (*flags & _PAGE_PRESENT) != 0;
}

static bool __init lkm2_cp_content_resolve(uintptr_t va, uint64_t *pa,
					   uint64_t *flags)
{
	uint64_t base, offset, mask;
	unsigned int shift;

	if (!lkm2_cp_content_walk(va, &base, flags, &shift) ||
	    (shift != PUD_SHIFT && shift != PMD_SHIFT && shift != PAGE_SHIFT))
		return false;
	mask = (1ULL << shift) - 1;
	offset = va & mask;
	if (base & mask || base > ~0ULL - offset ||
	    !(*flags & _PAGE_PRESENT) || !(*flags & _PAGE_LEAF) ||
	    (*flags & _PAGE_WRITE && !(*flags & _PAGE_READ)))
		return false;
	*pa = base + offset;
	return true;
}

static void __init lkm2_cp_content_push(struct lkm2_cp_content *state,
						uint64_t va, uint64_t pa, uint64_t flags)
{
	uint64_t index = state->count;
	flags &= state->class_id == 1 ? 0xff : _PAGE_PRESENT | _PAGE_READ;
	lkm2_cp_content_word(&state->lo, &state->hi, va);
	lkm2_cp_content_word(&state->lo, &state->hi, pa);
	lkm2_cp_content_word(&state->lo, &state->hi, flags);
	state->count++;
	if (lkm2_cp_diag_class != state->class_id)
		return;
	if (lkm2_cp_diag_stage == 1) {
		lkm2_cp_content_word(&state->diagnostic_lo, &state->diagnostic_hi, va);
		lkm2_cp_content_word(&state->diagnostic_lo, &state->diagnostic_hi, pa);
		lkm2_cp_content_word(&state->diagnostic_lo, &state->diagnostic_hi, flags);
		state->diagnostic_count++;
		if (state->diagnostic_count == 512) {
			lkm2_cp_content_word(&state->diagnostic_lo, &state->diagnostic_hi,
					     state->diagnostic_count);
			lkm2_checkpoint_content_chunk(state->class_id, state->diagnostic_chunk,
						state->diagnostic_count, state->diagnostic_lo,
						state->diagnostic_hi);
			state->diagnostic_chunk++;
			lkm2_cp_content_diag_begin(state);
		}
	} else if (lkm2_cp_diag_stage == 2 && index / 512 == lkm2_cp_diag_chunk) {
		lkm2_checkpoint_content_item(state->class_id, index, va, pa, flags);
	}
}

static void __init lkm2_cp_content_finish(struct lkm2_cp_content *state)
{
	if (lkm2_cp_diag_class == state->class_id && lkm2_cp_diag_stage == 1 &&
	    state->diagnostic_count) {
		lkm2_cp_content_word(&state->diagnostic_lo, &state->diagnostic_hi,
				     state->diagnostic_count);
		lkm2_checkpoint_content_chunk(state->class_id, state->diagnostic_chunk,
					state->diagnostic_count, state->diagnostic_lo,
					state->diagnostic_hi);
		state->diagnostic_count = 0;
	}
	lkm2_cp_content_word(&state->lo, &state->hi, state->count);
}

static struct lkm2_cp_content __init lkm2_cp_observe_content_fixmap(void)
{
	struct lkm2_cp_content result = lkm2_cp_content_begin(1);
	uintptr_t va = __fix_to_virt(FIX_FDT);
	unsigned int i;
	/* The final FDT window is two PMD leaves; project each to 4 KiB. */
	for (i = 0; i < 2 * PMD_SIZE / PAGE_SIZE; i++, va += PAGE_SIZE) {
		uint64_t pa, flags;
		if (!lkm2_cp_content_resolve(va, &pa, &flags)) {
			result.valid = false;
			break;
		}
		lkm2_cp_content_push(&result, va, pa, flags);
	}
	lkm2_cp_content_finish(&result);
	return result;
}

static struct lkm2_cp_content __init lkm2_cp_observe_content_linear(void)
{
	struct lkm2_cp_content result = lkm2_cp_content_begin(2);
	uint64_t start, end, i, pa;
	for_each_mem_range(i, &start, &end) {
		for (pa = start; pa < end; pa += PAGE_SIZE) {
			uintptr_t va = (uintptr_t)__va(pa);
			uint64_t mapped_pa, flags;
			if (!lkm2_cp_content_resolve(va, &mapped_pa, &flags) ||
			    mapped_pa != pa || !(flags & _PAGE_READ) ||
			    (flags & _PAGE_USER)) {
				result.valid = false;
				break;
			}
			lkm2_cp_content_push(&result, va, mapped_pa, flags);
		}
		if (!result.valid)
			break;
	}
	lkm2_cp_content_finish(&result);
	return result;
}

static uint64_t __init lkm2_cp_observe_content_kernel(void)
{
	uintptr_t va, end = kernel_map.virt_addr + kernel_map.size;
	if (end < kernel_map.virt_addr || !kernel_map.size ||
	    kernel_map.virt_addr & (PAGE_SIZE - 1))
		return 0;
	for (va = kernel_map.virt_addr; va < end; va += PAGE_SIZE) {
		uint64_t pa, flags, expected = kernel_map.phys_addr +
			(va - kernel_map.virt_addr);
		if (expected < kernel_map.phys_addr ||
		    !lkm2_cp_content_resolve(va, &pa, &flags) || pa != expected ||
		    (flags & _PAGE_USER) || (flags & _PAGE_WRITE && flags & _PAGE_EXEC))
			return 0;
	}
	return 1;
}

static struct lkm2_cp_content __init lkm2_cp_observe_content_fixmap_final(void)
{
	return lkm2_cp_observe_content_fixmap();
}

static struct lkm2_cp_content __init lkm2_cp_observe_content_linear_final(void)
{
	return lkm2_cp_observe_content_linear();
}

static struct lkm2_cp_content_observation __init lkm2_cp_observe_content(void)
{
	struct lkm2_cp_content fixmap, linear;
	lkm2_cp_diag_configure();
	fixmap = lkm2_cp_observe_content_fixmap_final();
	linear = lkm2_cp_observe_content_linear_final();
	return (struct lkm2_cp_content_observation) {
		.fixmap_valid = fixmap.valid, .fixmap_count = fixmap.count,
		.fixmap_digest_lo = fixmap.lo, .fixmap_digest_hi = fixmap.hi,
		.linear_valid = linear.valid, .linear_count = linear.count,
		.linear_digest_lo = linear.lo, .linear_digest_hi = linear.hi,
		.kernel_walk_valid = lkm2_cp_observe_content_kernel(),
	};
}

static void __init lkm2_checkpoint_swapper_content(void)
{
	struct lkm2_cp_content_observation content = lkm2_cp_observe_content();
	lkm_checkpoint_swapperpagetable_content_fixmap_1b5e2b9649895da5(content.fixmap_valid, content.fixmap_count, content.fixmap_digest_lo, content.fixmap_digest_hi);
	lkm_checkpoint_swapperpagetable_content_linear_8d9969b8ad2f73e1(content.linear_valid, content.linear_count, content.linear_digest_lo, content.linear_digest_hi);
	lkm_checkpoint_swapperpagetable_content_kernel_walk_valid_3eebd41d30f56760(content.kernel_walk_valid);
}
